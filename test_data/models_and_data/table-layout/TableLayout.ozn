output [(((("("++show(cellheight[r,c]))++",")++show(cellwidth[r,c]))++")")++if c==cols then "\n" else " " endif | r in ROWS, c in COLS]++["objective = ",show(totalheight),"\n"];
set of int: COLS = 1..20;
set of int: ROWS = 1..500;
array [1..10000] of int: X_INTRODUCED_20005_;
array [ROWS,COLS] of int: cellheight = array2d(1..500,1..20,X_INTRODUCED_20005_);
array [1..10000] of int: X_INTRODUCED_10004_;
array [ROWS,COLS] of int: cellwidth = array2d(1..500,1..20,X_INTRODUCED_10004_);
int: cols = 20;
array [ROWS] of int: rowheight;
int: totalheight = sum([rowheight[r] | r in ROWS]);
